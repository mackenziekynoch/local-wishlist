import { Entity } from '../types';

export class EntityService {
  constructor() {}

  getAll() {}
  getById(id: string) {}
  getByProperty(property: string, value: any) {}

  updateById(id: string, entity: Entity) {}

  create(entity: Entity) {}

  deleteById(id: string) {}
};